pipeline {
    agent any
  
    stages {
        stage('checkout') {
            steps {
                git 'https://github.com/kelvin-alef/react-app.git' branch: 'main'
            }
        }
    }
}
