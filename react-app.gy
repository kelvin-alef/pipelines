pipeline {
    agent any
  
    stages {
        stage('Checkout repository') {
            steps {
                git url: 'https://github.com/kelvin-alef/react-app.git', branch: 'main'
            }
        }
    }
}
